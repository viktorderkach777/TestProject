pipeline {


agent any 
  parameters {
        string(name: 'TestCategory', defaultValue: 'Login', description: 'Enter the testcategory')
    }

environment {
act = 'mmmmmmm'
act2 = 'act2'
act3 = 'act3'
dotnet = '/usr/share/dotnet/dotnet'
length='length'

}

stages {  

stage('Checkout') {

steps {

checkout scm


}

}

stage('Restore Packages') {

steps {

dir("awesome-project") {

sh "ls -la"

sh "pwd"

sh 'dotnet --info'

sh 'dotnet restore'

}

}

}

stage('DotNet Test') {

steps {

dir("awesome-project") {

sh "ls -la"

sh "pwd"
  script {
               act = currentBuild.getBuildCauses('hudson.model.Cause$UserIdCause')!= null
               act2 = currentBuild.getBuildCauses('hudson.model.Cause$UserIdCause')
               act3 = act2.toString().equals("[]");
               length = (params.TestCategory).length()
          }
echo "${currentBuild.getBuildCauses('jenkins.branch.BranchEventCause')}"   
echo "${currentBuild.getBuildCauses('hudson.model.Cause$UserIdCause')}" 

sh 'dotnet --info'

  echo "${params.TestCategory}" 
   echo "${act}" 
   echo "${act2}" 
   echo "${act3}" 
  echo "${currentBuild.getBuildCauses('hudson.model.Cause$UserIdCause')}" 
  echo "${length}"
}

}

}  
 

stage('Example Deploy') {
            when {
                triggeredBy 'SCMTrigger'
            }
            steps {
                echo "============SCMTrigger==========================="
            }
        }


}

}
